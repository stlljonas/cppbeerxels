cmake_minimum_required(VERSION 2.8)
project( cppbeerxels ) 
find_package( OpenCV REQUIRED)
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( cppbeerxels src/main.cpp )
target_link_libraries( cppbeerxels ${OpenCV_LIBS} )
